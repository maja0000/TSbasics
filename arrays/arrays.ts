let cars = ['citroen', 'fiat', 'chevy'];
// let cars: string[] = ['citroen', 'fiat', 'chevy'];

let carsProduc: string[][];
// array of arrays with strings (type only if initialised but not assigned value)
carsProduc = [['sss'], ['dddd'], ['fff']];

let importantDates: (Date | string)[] = [new Date(), 'myBirthday '];
// console.log(importantDates);
